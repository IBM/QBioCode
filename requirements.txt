# ==========================================================================
# QBioCode requirements -- pointer file
# ==========================================================================
# The dependency lists moved into requirements/ and are now tiered:
#
#   requirements/requirements-base.txt    core runtime (read by pyproject.toml)
#   requirements/requirements-quvine.txt  the [quvine] extra
#   requirements/requirements-docs.txt    the [docs] extra
#   requirements/requirements.txt         everything, for development
#
# This file is kept so that the documented
#     pip install -r requirements.txt
# still installs a complete development environment. Run it from the repo
# root -- requirements/requirements.txt ends in "-e .[dev]", which pip
# resolves relative to the current directory.
# ==========================================================================

-r requirements/requirements.txt
